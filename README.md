# Controller Module

## Overview

This branch implements a controller for a mobile robot in **ROS 2 Humble**.  
After the users incrementally select waypoints, the controller follows the generated smooth path with defined **velocity** and/or **acceleration**.

---
